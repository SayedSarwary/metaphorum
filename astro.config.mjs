// @ts-check
// import { defineConfig } from "astro/config";

// export default defineConfig({
//     output: "static",
// });

import { defineConfig } from "astro/config";

export default defineConfig({
    site: "https://SayedSarwary.github.io",
    base: "/metaphorum/",
});
